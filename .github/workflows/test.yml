




#name: "first Hello World"
##actionとして何が実行されているか
##all workflows
#run-name: "run_name set "
#
#on: [push]
#
#jobs:
#  test:
#    runs-on: ubuntu-latest
#
#    #workflow の中のどの作業か。
#    name: Hello World Job
#    steps:
#      - run: echo "Hello World!"



## user名
#${{ github.actor }} 
#anbergon


## triger name
#${{ github.event_name }} 
#push
 
## OS名
#${{ running.os }}
#linux


## branch 
# ${{ github.ref }}
# refs/heads/branch name
#
# ${{ github.ref_name }}
# branch_name


## repository
#${{ github.repository }}."
#ambergon/testGitAction

## 既存のワークフローの再利用
# uses: actions/checkout@v3
# uses: user/branch@tag or commit_num
# {owner}/{repo}/.github/workflows/{filename}@{ref}パブリックとプライベートリポジトリの再利用可能ワークフローの 。
# ./.github/workflows/{filename}は同じリポジトリ内の再利用可能なワークフローの場合。

## git checkout
#- name: Check out repository code
#  uses: actions/checkout@v3

#下記のコードはチェックアウトしないと中のファイルにアクセスできない。
#- name: List files in the repository
#  run: ls ${{ github.workspace }}


# 横に表示される名前
name: GitHub Actions Demo

#ambergon is testing out GitHub Actions
#work flow名
run-name: ${{ github.actor }} is testing out GitHub Actions 
on: [push]
jobs:
  #job idか。
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      #実行ごとに表示される名前。
      - name: List files in the repository
        run: ls ${{ github.workspace }}
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: List files in the repository
        run: ls ${{ github.workspace }}
        run: ls
          #run: ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."





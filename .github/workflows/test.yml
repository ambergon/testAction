name: "Hello World"

# 動くきっかけ
on: [push]

# 動かす内容
jobs:
  sample:
    name: Hello World Job
    # 実行環境
    runs-on: ubuntu-latest
    # 実行する内容
    steps:
      - run: ls -la ./github
      - run: ls
      - run: echo "Hello World!"

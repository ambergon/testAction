




name: "first Hello World"
run-name: "run_name set "

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: "Hello World Job 1"
      - run: echo "Hello World! 1"
      - name: "Hello World Job 2"
      - run: echo "Hello World! 2"

    # 実行環境
    # 実行する内容

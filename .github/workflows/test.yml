










#work flow名
run-name: ${{ github.actor }} is testing out GitHub Actions 
on: [push]
jobs:
  #job name
  Your-Job-ID:
    #use os
    runs-on: ubuntu-latest
    steps:
      ##実行ごとに表示される名前。
      #check out
      - name: Check out repository code
        uses: actions/checkout@v3

      - run: ls
      - run: sh a.sh >> anko.md
      - run: cat anko.md
      - run: echo "${{ job.status }}"
        #反映したいな。
        #
      - name: check git
      - run: |
      git config --global user.email "sc.soar@gmail.com"
      git config --global user.name  "ambergon workflow"
      git add ./anko.md
      git commit -m "[add] anko.md"
      git status


